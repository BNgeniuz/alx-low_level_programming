#include "main.h"
/**
 * mul - a fnxtn to multiplies two integers
 * @a: first var
 * @b: second var
 * Return: (1) for correct otherwise (0)
 */

int mul(int a, int b)
{
	int mul;

	return (a * b);
	mul = a * b;
	{
		return (mul);
	}
}
