#include <stdio.h>
/**
 * main - program that prints the lowercase alphabet in reverse
 *
 * Return: 0 (Good)
 */
int main(void)
{
	char h;

	for (h = 'z' ; h >= 'z' ; h++)
		putchar(h);
	putchar('\n');
	return (0);
}
